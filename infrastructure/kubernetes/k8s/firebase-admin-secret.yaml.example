# Firebase Admin Secret - EXAMPLE TEMPLATE
# DO NOT commit actual credentials to git!
#
# To create this secret:
# 1. Get your Firebase Admin SDK key from Firebase Console
# 2. Base64 encode it: cat firebase-admin-key.json | base64
# 3. Replace <BASE64_ENCODED_KEY> below
# 4. Save as firebase-admin-secret.yaml (without .example)
# 5. Apply: kubectl apply -f firebase-admin-secret.yaml
#
# RECOMMENDED: Use External Secrets Operator instead of manual secrets
# See: infrastructure/kubernetes/k8s/external-secrets.yaml

apiVersion: v1
kind: Secret
metadata:
  name: firebase-admin-key
  namespace: cloud-secrets-manager
  labels:
    app.kubernetes.io/part-of: cloud-secrets-manager
type: Opaque
data:
  firebase-admin-key.json: <BASE64_ENCODED_KEY>
