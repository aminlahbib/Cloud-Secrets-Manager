apiVersion: apps/v1
kind: Deployment
metadata:
  name: csm-secret-service
  namespace: cloud-secrets-manager
  labels:
    app.kubernetes.io/name: secret-service
    app.kubernetes.io/component: api
    app.kubernetes.io/part-of: cloud-secrets-manager
spec:
  # For production HA: increase to 2+ and add a PodDisruptionBudget
  replicas: 1
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app.kubernetes.io/name: secret-service
      app.kubernetes.io/component: api
  template:
    metadata:
      labels:
        app.kubernetes.io/name: secret-service
        app.kubernetes.io/component: api
        app.kubernetes.io/part-of: cloud-secrets-manager
    spec:
      serviceAccountName: secret-service
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: cloud-sql-proxy
          image: gcr.io/cloud-sql-connectors/cloud-sql-proxy:2.8.0
          args:
            - "--structured-logs"
            - "--port=5432"
            - "cloud-secrets-manager:europe-west10:secrets-manager-db-dev-3631da18"
          securityContext:
            runAsNonRoot: true
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            seccompProfile:
              type: RuntimeDefault
          resources:
            requests:
              memory: "64Mi"
              cpu: "30m"
            limits:
              memory: "128Mi"
              cpu: "100m"

        # For production: pin to a git-SHA or semver tag instead of :latest
        - name: secret-service
          image: europe-west10-docker.pkg.dev/cloud-secrets-manager/docker-images/secret-service:latest
          imagePullPolicy: Always
          securityContext:
            runAsNonRoot: true
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            seccompProfile:
              type: RuntimeDefault
          env:
            - name: SPRING_DATASOURCE_URL
              value: jdbc:postgresql://127.0.0.1:5432/secrets?sslmode=disable
            - name: SPRING_DATASOURCE_USERNAME
              valueFrom:
                secretKeyRef:
                  name: csm-db-secrets
                  key: secrets-db-user
            - name: SPRING_DATASOURCE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: csm-db-secrets
                  key: secrets-db-password
            - name: SPRING_PROFILES_ACTIVE
              value: prod
            - name: SPRING_JPA_HIBERNATE_DDL_AUTO
              value: validate
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: csm-app-config
                  key: JWT_SECRET
                  optional: true
            - name: ENCRYPTION_KEY
              valueFrom:
                secretKeyRef:
                  name: csm-app-config
                  key: AES_KEY
                  optional: true
            - name: AUDIT_SERVICE_URL
              value: http://csm-audit-service:8081
            - name: GOOGLE_IDENTITY_ENABLED
              value: "true"
            - name: GOOGLE_PROJECT_ID
              value: "cloud-secrets-manager"
            - name: GOOGLE_API_KEY
              valueFrom:
                secretKeyRef:
                  name: csm-app-config
                  key: GOOGLE_API_KEY
                  optional: true
            - name: SETUP_ENABLED
              value: "false"
          ports:
            - containerPort: 8080
              name: http
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8080
            initialDelaySeconds: 60
            periodSeconds: 15
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          resources:
            requests:
              memory: "512Mi"
              cpu: "300m"
            limits:
              memory: "1Gi"
              cpu: "1000m"
---
apiVersion: v1
kind: Service
metadata:
  name: csm-secret-service
  namespace: cloud-secrets-manager
  labels:
    app.kubernetes.io/name: secret-service
    app.kubernetes.io/component: api
    app.kubernetes.io/part-of: cloud-secrets-manager
spec:
  selector:
    app.kubernetes.io/name: secret-service
    app.kubernetes.io/component: api
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  type: ClusterIP
