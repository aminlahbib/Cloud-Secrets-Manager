apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-service
  labels:
    {{- include "csm.labels" . | nindent 4 }}
    app.kubernetes.io/name: notification-service
spec:
  replicas: {{ .Values.notificationService.replicas }}
  selector:
    matchLabels:
      app.kubernetes.io/name: notification-service
  template:
    metadata:
      labels:
        app.kubernetes.io/name: notification-service
    spec:
      serviceAccountName: notification-service
      containers:
        - name: notification-service
          image: {{ include "csm.image" (dict "registry" .Values.global.image.registry "name" .Values.notificationService.image.name "tag" .Values.global.image.tag) }}
          imagePullPolicy: {{ .Values.notificationService.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.notificationService.port }}
              name: http
          env:
            {{- range $k, $v := .Values.notificationService.env }}
            - name: {{ $k }}
              value: {{ $v | quote }}
            {{- end }}
            - name: SPRING_DATASOURCE_URL
              value: "jdbc:postgresql://localhost:5432/secrets"
            - name: SPRING_DATASOURCE_USERNAME
              valueFrom:
                secretKeyRef:
                  name: db-secrets
                  key: secrets-user
            - name: SPRING_DATASOURCE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: db-secrets
                  key: secrets-password
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: app-secrets
                  key: jwt-secret
            - name: GCP_PROJECT_ID
              value: {{ .Values.global.projectId | quote }}
            - name: PUBSUB_SUBSCRIPTION
              value: {{ .Values.pubsub.subscriptionName | quote }}
          resources:
            requests:
              cpu: {{ .Values.notificationService.resources.requests.cpu }}
              memory: {{ .Values.notificationService.resources.requests.memory }}
            limits:
              cpu: {{ .Values.notificationService.resources.limits.cpu }}
              memory: {{ .Values.notificationService.resources.limits.memory }}
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: http
            initialDelaySeconds: 30
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: http
            initialDelaySeconds: 60
            periodSeconds: 15
        {{- include "csm.cloudSqlProxy" . | nindent 8 }}
---
apiVersion: v1
kind: Service
metadata:
  name: notification-service
  labels:
    {{- include "csm.labels" . | nindent 4 }}
    app.kubernetes.io/name: notification-service
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.notificationService.port }}
      targetPort: http
      name: http
  selector:
    app.kubernetes.io/name: notification-service
